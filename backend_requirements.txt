# 后端 Python 依赖
# 核心依赖
# 安装位置：Python 解释器的 site-packages 目录
pyautogui          # 用于自动化鼠标和键盘操作
opencv-python      # 用于图像处理
numpy              # 用于数值计算
onnxruntime        # 用于运行 ONNX 模型
Pillow             # 用于图像处理
pywin32            # 用于 Windows 系统操作
psutil             # 用于系统进程管理

# OCR 依赖
# 安装位置：Python 解释器的 site-packages 目录
rapidocr-onnxruntime  # 用于 OCR 文字识别
